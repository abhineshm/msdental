# Website Photos

Store all static images for the site in this folder.

Use them in the app with a root-relative path, for example:

```tsx
<img src="/images/doctor.jpg" alt="Doctor" />
```

This works because files inside `public/` are served directly by Vite at the website root.
